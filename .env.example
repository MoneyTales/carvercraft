# Firebase Realtime Database URL (multiplayer signaling).
# Leave empty to fall back to free public MQTT brokers (zero config).
VITE_FIREBASE_RTDB_URL=

# Cloudflare TURN credentials (optional, NAT traversal fallback)
VITE_TURN_TOKEN_ID=
VITE_TURN_API_TOKEN=
